C({"name": "jQuery.Controller.static.processors", "type": "attribute", "parent": "jQuery.Controller.static", "comment": "<p>An object of {eventName : function} pairs that Controller uses to hook up events\nauto-magically.  A processor function looks like:</p>\n\n<pre><code>jQuery.Controller.processors.\n  myprocessor = function( el, event, selector, cb, controller ) {\n     //el - the controller's element\n     //event - the event (myprocessor)\n     //selector - the left of the selector\n     //cb - the function to call\n     //controller - the binding controller\n  };\n</code></pre>\n\n<p>This would bind anything like: \"foo~3242 myprocessor\".</p>\n\n<p>The processor must return a function that when called, \nunbinds the event handler.</p>", "children": ["jQuery.Controller.static.processors.subscribe"]})