/ ALBUMS

#albumList.hidden

  %table
    - @albums._?.each do |album, images|

      %tr.name
        %td
          = album || "All Images"

          %a.manage(href="#{manage_images_path}")
          /?ids=#{images.map(&:id).join(',')}

      %tr.images
        %td
          - images.each do |image|
            - metaData = ''
            .img{:'data-id' => image.id}
              %img{id: "image_#{image.id}", src: image.url("126x126")}
              .add.hidden +








