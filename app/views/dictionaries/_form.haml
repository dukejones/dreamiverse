= form_for(dict) do |f|
  %br
  .name
    = f.label :name
    %br
    = f.text_field :name, :class => 'font-21'
  .attr
    = f.label :attribution
    %br
    = f.text_field :attribution, :class => 'font-21'
  = f.submit :class => 'submit', :style => 'width: 100%; margin-top: 20px'
