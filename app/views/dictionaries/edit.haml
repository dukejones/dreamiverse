- content_for :head do
  = sass_link_tag 'dictionaries'

.container.light

  .module
    %h1
      edit dictionary
      %a{href: dictionary_words_path(@dict)}
        &lsaquo; cancel

    = render({ partial: 'form', locals: { dict: @dict } })