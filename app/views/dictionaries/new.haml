- content_for :head do
  = sass_link_tag 'dictionaries'

.container.light

  .module
    %h1
      new dictionary
      = link_to "< cancel", dictionaries_path
    = render({ partial: 'form', locals: { dict: @dict } })
