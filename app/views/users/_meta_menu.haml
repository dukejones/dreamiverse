:coffeescript
  require({baseUrl: "/javascripts/compiled"}, ['metamenu', 'dreamplugs'])


#report_box= VERSION

%a#logo{:href => (current_user ? user_entries_path(current_user.username) : root_path) }
  %img{src: '/images/logos/dreamcatcher_logo-THETA.png', alt: 'Dreamcatcher', border: '0'}



- if current_user.blank?
  / META MENU - PUBLIC
  = render :partial => "users/meta_menu_public"

- else
  / META MENU - USER
  = render :partial => "users/meta_menu_user"